# Load packages -----------------------------------------------------
library(shiny)
library(tidyverse)
# add any other packages you need here

# Load data ---------------------------------------------------------
# add code for loading your data here

# Define UI ---------------------------------------------------------
ui <- fluidPage(
	)

# Define server function --------------------------------------------
server <- function(input, output) {

}

# Create the Shiny app object ---------------------------------------
shinyApp(ui = ui, server = server)
